require 'spec_helper'

describe HomeController do

  describe "GET index without login" do
    #it "should redirect to log in when user is not logged in" do
    #  get :index
    #  response.should be_redirect
    #  response.should redirect_to(log_in_path)
    #end
  end

end
