<% content_for :sidebar do %>
    <h1><%= t('document.title') %></h1>
    <p>
    <%= link_to t('document.new'), new_document_path %>
    </p>
    <%= render "filters" %>
<% end %>

<h1><%= t('document.title') %></h1>

<%= render "documents" %>